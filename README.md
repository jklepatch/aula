# Aula Interview Assignment

Music player with React + Nodejs (so far only the backend in Nodejs is implemented)

## Get started

Requirements: Nodejs 8+

Install dependencies:

```
npm install
```

Run the backend:

```
npm start
```
